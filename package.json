{
  "name": "credit-score-dashboard",
  "version": "1.0.0",
  "description": "Credit Score Dashboard Application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run server --workspace=backend\"",
    "start": "npm run start --workspace=backend",
    "build": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "comlink": "^4.4.2",
    "joi": "^17.13.3",
    "rate-limit-redis": "^4.2.1",
    "redis": "^5.5.6",
    "sanitize-html": "^2.17.0"
  }
}
